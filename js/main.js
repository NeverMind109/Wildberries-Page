const swiper = new Swiper('.swiper', {
    // Optional parameters
    loop: true,
    speed: 600,
 
    // Navigation arrows
    navigation: {
      nextEl: '.slider-button-next',
      prevEl: '.slider-button-prev',
    },
  });